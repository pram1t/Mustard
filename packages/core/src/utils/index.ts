/**
 * Utility exports
 *
 * Re-exports from @pram1t/mustard-logger for backward compatibility.
 */

export {
  DEFAULT_SAFE_ENV_VARS,
  BLOCKED_ENV_VARS,
  filterEnvVars,
} from '@pram1t/mustard-logger';

export type { FilterEnvOptions } from '@pram1t/mustard-logger';
