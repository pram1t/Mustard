{
  "name": "@mustard/orchestrator",
  "version": "0.0.0",
  "private": true,
  "description": "Multi-worker orchestrator for OpenAgent V2",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mustard/core": "*",
    "@mustard/llm": "*",
    "@mustard/tools": "*",
    "@mustard/logger": "*",
    "@mustard/message-bus": "*",
    "@mustard/worker": "*",
    "@mustard/memory": "*",
    "@mustard/artifact": "*",
    "@mustard/queue": "*"
  },
  "devDependencies": {
    "@types/node": "^25.0.9",
    "typescript": "^5.9.3"
  }
}
